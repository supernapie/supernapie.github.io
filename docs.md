# Documentation

## Programming interface

Events
- `on`
- `off`
- `once`
- `emit`
- `last`

Event types

- `update` `{ t, dt }`
- `draw` `{ ctx }`
- `tap` `{ x, y }`
- `color` `{ bg, fill, stroke }`
- `resize` `{ vw, vh, vc }`